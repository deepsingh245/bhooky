// Builders Club v1 hard cap on order total — see BHOOKY_BUILD_PLAN.md §8.
export const MAX_ORDER_TOTAL_RUPEES = 1000;

// Swiggy's stated minimum poll interval for track_food_order.
export const TRACK_ORDER_MIN_POLL_INTERVAL_MS = 10_000;
